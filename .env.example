# Optional: gate the chat endpoint behind a shared access code so only people
# with the code can spend your AI credits. Leave unset to disable the gate.
# Use a long, random value (e.g. `openssl rand -base64 24`).
ACCESS_CODE=

# Required: default provider (GPT-4o mini / GPT-4o)
OPENAI_API_KEY=

# Optional: enables Gemini models in the model picker
GOOGLE_GENERATIVE_AI_API_KEY=

# Optional: enables OpenRouter models in the model picker
# (lib/@ai-sdk/openrouter.ts)
OPENROUTER_API_KEY=

# Optional: enables the webSearch tool (https://tavily.com/)
TAVILY_API_KEY=
